### PD 민준 (Opening)
좋습니다. 일곱 번째 회의를 시작하겠습니다.

---

## Meeting #7 — 킥오프 정상화

솔직히 말씀드리겠습니다. 지난 6번의 회의에서 결정사항도, 할당된 태스크도 없었습니다. 이건 문제입니다. 오늘은 반드시 구체적인 결과물을 가지고 나가겠습니다.

---

### 팀 현황 리뷰

- **서진**: 코드 구조 리뷰와 회의 시스템 백엔드 준비 상태. cli-runner, agent-pool, db schema 파악이 필요하다고 했죠.
- **하은**: 기존 설계 문서 리뷰와 사용자 시나리오 작성 대기 중. 기능 우선순위 정리가 필요한 상태.
- **도윤**: 클라이언트 UI 구조 파악과 회의 진행 화면 설계 구상 중. 픽셀아트 오피스와의 통합이 열린 질문.

공통적으로 "파악", "준비", "구상" 단계에 멈춰 있습니다. 오늘 이걸 "실행"으로 전환합니다.

---

### 이번 회의 핵심 안건

**"첫 번째 동작하는 기능을 이번 주 안에 만들자."**

Plan과 Design은 완료 상태입니다. 이제 구현(DO)에 들어가야 합니다. 목표는 단순합니다:

1. **에이전트 대시보드 기본 화면** — 에이전트 목록이 보이고, 상태가 실시간으로 반영되는 것
2. **에이전트 실행 기능** — UI에서 에이전트를 선택하고 프롬프트를 보내서 실행할 수 있는 것
3. **결과 표시** — 실행 결과가 WebSocket으로 스트리밍되어 화면에 나타나는 것

회의 시스템은 그 다음입니다. 기본 동작이 먼저.

---

### 팀원별 요청

**서진에게**: 현재 `packages/server/src/`의 cli-runner와 agent-pool 코드가 어느 정도 구현되어 있습니까? 에이전트 실행 → WebSocket 스트리밍까지 돌아가는 상태인지, 아니면 스켈레톤만 있는 상태인지 솔직하게 알려주세요. 거기서부터 시작점을 잡겠습니다.

**하은에게**: `docs/02-design/features/web-dashboard.design.md`를 기준으로, Phase 1에서 반드시 들어가야 할 기능 3개만 뽑아주세요. "있으면 좋겠다"가 아니라 "이것 없으면 대시보드가 아니다" 수준으로. 오늘 회의 끝나기 전에 확정합니다.

**도윤에게**: 픽셀아트 오피스 화면의 현재 상태가 궁금합니다. 캔버스 렌더링이 돌아가는 상태입니까? 에이전트 캐릭터가 화면에 나타나기까지 어떤 작업이 남았는지 정리해주세요. 회의 장면은 나중이고, 일단 에이전트가 오피스에서 보이는 것이 먼저입니다.

---

각자 현재 상태를 정직하게 공유해주시고, 오늘 회의에서 이번 주 목표와 각자의 담당을 확정짓겠습니다.

```json
{"agenda": ["지난 6회 회의 무성과 원인 점검", "현재 코드베이스 실제 구현 상태 확인", "Phase 1 필수 기능 3개 확정", "이번 주 팀원별 구체 태스크 할당 (담당자 + 완료 기준)"]}
```

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: meeting-agent (팀 회의 시뮬레이션), SessionStart Hook
⏭️ Not Used: /pdca do (회의 시뮬레이션 진행 중, 구현은 회의 결론 후), gap-detector (아직 구현 전)
💡 Recommended: 회의 결론 후 /pdca do store 로 구현 단계 진입
─────────────────────────────────────────────────

### 하은 (planner)
설계 문서 읽었습니다. 바로 정리할게요.

---

### Phase 1 필수 기능 3개 (순서도 중요합니다)

설계 문서 WHY부터 봤습니다: **"무엇이 돌고 있는지, 어디까지 진행됐는지 한눈에 알 수 없음"**

여기서 역산하면 답이 나옵니다.

---

**1순위: 에이전트 목록 + 실시간 상태**
(`AgentListView` + `StatusBadge` + `statusUpdate` WebSocket)

"무엇이 돌고 있는지" — 이게 없으면 대시보드가 아니라 그냥 빈 화면입니다.
- idle / working / error / queued 4가지 상태가 실시간으로 바뀌어야 함
- **성공 기준**: WebSocket 연결 후 3초 이내에 에이전트 목록 표시

---

**2순위: 에이전트 실행**
(`CommandBar`: 에이전트 선택 → 프롬프트 → 실행)

"직접 작업을 수행" — 이게 없으면 모니터링 화면이지 통합 대시보드가 아닙니다.
- **성공 기준**: 에이전트 선택 → 프롬프트 입력 → Enter → 202 응답 + 상태가 working으로 전환

---

**3순위: 실행 결과 스트리밍**
(`LogPanel` + `agentStream` / `agentDone` WebSocket)

"어디까지 진행됐는지" — 이게 없으면 CLI랑 차이가 없습니다.
- **성공 기준**: 실행 후 stdout이 청크 단위로 화면에 출력, 완료 시 tokensUsed / costUsd 표시

---

### 빠진 것 (명시적으로 Phase 1 제외 제안)

- **OfficePage (캔버스)**: WHY를 충족하는 건 테이블 뷰만으로 가능합니다. 픽셀아트는 차별화 포인트지 필수가 아님. Phase 1 제외를 제안합니다.
- **활동 타임라인**: 있으면 좋지만, 3번(LogPanel)이 있으면 대체 가능. Phase 1.5 이후.

---

### 하나만 확인할게요

민준 씨, 제가 3번에 "실행 결과 스트리밍"을 넣었는데요. 설계 문서에 `LogPanel`이 페이지 고정인지 슬라이딩 패널인지 명시가 없습니다. 서진 씨, 현재 `cli-runner`에서 stdout을 청크 단위로 WS에 내보내는 부분이 구현되어 있나요? 이 부분이 안 되어 있으면 3번 기능의 구현 난이도가 완전히 달라집니다. 시작점을 정하려면 이게 먼저 필요합니다.

---

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: Design 문서 리뷰 (web-dashboard.design.md), 사용자 시나리오 기반 기능 분석
⏭️ Not Used: /pdca do (회의 중), gap-detector (아직 구현 전)
💡 Recommended: 서진 답변 후 기능 3개 확정 → /pdca do store 진입
─────────────────────────────────────────────────

### 서진 (developer)
코드 직접 읽었습니다. 솔직하게 현황 보고합니다.

---

### 서진 (developer)

민준 씨, 하은 씨 — 좋은 소식과 나쁜 소식이 있습니다.

**좋은 소식부터.**

하은 씨가 물어본 `cli-runner.ts`의 stdout → WebSocket 스트리밍, **이미 구현되어 있습니다.** 확인한 내용:

```
cli-runner.ts:86-119  → proc.stdout.on('data') → JSON 파싱 → broadcast({ type: 'agentStream', ... })
cli-runner.ts:124-128 → stderr도 agentStream으로 브로드캐스트
agent-pool.ts:31-35   → p-limit 큐 → queued → working → runCli 순서 완전 동작
```

서버 파이프라인은 **실행 가능한 상태**입니다. `AgentListView`, `CommandBar`, `LogPanel`, `useAgentStore` — 클라이언트 컴포넌트도 전부 구현되어 있습니다.

---

**나쁜 소식.**

이것들이 **하나의 페이지에 조립되어 있지 않습니다.**

`App.tsx` 라우트를 보면:
```
/          → OverviewPage  (통계 카드 + 스킬 목록만)
/office    → OfficePage
/activity  → ActivityPage
/settings  → SettingsPage
```

`AgentListView`와 `LogPanel`을 함께 보여주는 `/agents` 페이지가 없습니다. `DashboardShell`에 `CommandBar`는 전역 마운트되어 있는데, 에이전트를 선택하고 실행해도 로그를 볼 수 있는 화면이 없는 상태입니다.

---

### 따라서 Phase 1의 실제 작업량

하은 씨가 정의한 3가지 기능 — 전부 동의합니다. 그리고 이미 **부품은 다 있습니다.** 조립만 하면 됩니다.

구체적으로 필요한 건:

1. `AgentListView` + `LogPanel`을 조합한 `AgentsPage.tsx` 생성
2. `App.tsx`에 `/agents` 라우트 추가 (또는 `OverviewPage`를 교체)
3. `LogPanel`에 선택된 에이전트 청크 연결 (`useAgentStore.chunks[selectedAgentId]`)

**이게 전부입니다.** 신규 기능 개발이 아니라 **연결 작업**입니다. 오늘 오후에 끝낼 수 있습니다.

---

### 하은 씨 질문에 대한 답

`LogPanel`은 고정 패널이 맞습니다. `DashboardShell`에 하단 고정 `CommandBar`가 있고, `LogPanel`은 페이지 콘텐츠 영역 하단에 분할되어 붙는 구조가 자연스럽습니다. 슬라이딩은 과잉입니다. Phase 1에서는 불필요합니다.

---

### 제 이번 주 태스크 제안

**오늘**: `AgentsPage.tsx` 작성 + 라우트 연결 → `npm run build` 0 errors 확인  
**내일**: `npm run dev`로 실제 CLI 실행 → WebSocket 스트리밍 E2E 검증  

민준 씨, 다른 팀원들 이견 없으면 지금 바로 시작하겠습니다.

---

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: Read (cli-runner.ts, agent-pool.ts, AgentListView.tsx, LogPanel.tsx, CommandBar.tsx, useAgentStore.ts, App.tsx, DashboardShell.tsx 코드베이스 직접 분석)
⏭️ Not Used: /pdca do (회의 중 의사결정 단계), gap-detector (구현 전)
💡 Recommended: 회의 확정 후 → `AgentsPage.tsx` 구현 → `/pdca analyze store` 로 Gap 분석
─────────────────────────────────────────────────

### 도윤 (ui-designer)
서진 씨 분석 들으면서 안도했습니다. 부품이 다 있다는 거, 디자이너 입장에서는 최고의 소식입니다. 조립이 안 된 상태는 제가 제일 잘 압니다 — 화면 그려놓고 "근데 이 컴포넌트 언제 만들어요?" 물어봐야 하는 상황이 아니니까요.

---

### 캔버스/OfficePage 현황 (솔직하게)

민준 씨가 물어보신 것 — 에이전트 캐릭터가 오피스에서 보이려면 뭐가 남았냐는 건데, 지금 당장은 **하은 씨 말이 맞습니다. Phase 1 제외가 맞아요.**

이유를 기술적으로 말하는 게 아니라 사용자 경험으로 설명하면: 캔버스 위에서 에이전트 캐릭터가 움직이는 게 의미 있으려면 그 캐릭터가 "지금 뭐 하는지"를 알 수 있어야 합니다. 근데 그 정보 연결이 없는 상태에서 캐릭터만 화면에 띄우면 — 그냥 보기 좋은 빈 껍데기예요. 사용자가 "저 캐릭터가 에이전트인지 장식인지" 3초 안에 판단 못 합니다.

**오피스는 AgentsPage가 동작하고, 실시간 상태 연결이 검증된 다음에 얹는 게 순서입니다.**

---

### AgentsPage 레이아웃 — 지금 결정해야 할 것

서진 씨가 "AgentsPage.tsx 만들면 된다"고 했는데, 그 전에 레이아웃 방향 하나만 확정하고 가야 합니다. 안 그러면 서진 씨가 만들고 나서 제가 "이거 다시 짜야 해요" 하는 상황 나옵니다.

**제안하는 구조:**

```
┌─────────────────────────────────┐
│  AgentsPage                     │
├─────────────┬───────────────────┤
│             │                   │
│ AgentList   │   LogPanel        │
│ (좌 1/3)    │   (우 2/3)        │
│             │                   │
│ - 에이전트  │  선택된 에이전트의 │
│   카드 목록  │  스트리밍 로그    │
│ - 상태 뱃지  │                   │
│             │                   │
└─────────────┴───────────────────┘
│  CommandBar (하단 고정 — 기존)   │
└─────────────────────────────────┘
```

핵심은 **선택 → 즉시 오른쪽에 반영**입니다. 클릭하면 100ms 안에 LogPanel이 해당 에이전트 로그로 전환되어야 해요. 탭 이동이나 페이지 전환 없이.

**하나 더**: 에이전트 목록에서 현재 working 상태인 것은 **최상단에 고정**되어야 합니다. idle 에이전트 20개 사이에서 지금 돌고 있는 에이전트 찾으라고 하면 안 됩니다.

---

### 하은 씨 기능 우선순위에 한마디

동의합니다. 그리고 3번(스트리밍)을 단순히 "로그 출력"으로 구현하지 않았으면 합니다.

`agentDone` 이벤트 왔을 때 — tokensUsed, costUsd 이게 그냥 텍스트로 뜨면 사용자가 봐도 맥락이 없어요. LogPanel 하단에 **완료 요약 칩 하나**만 붙여주면 됩니다:

```
✓ 완료  |  2,340 tokens  |  $0.012  |  14.3s
```

서진 씨, 이거 `agentDone` 이벤트에 다 들어있죠? 컴포넌트 레벨에서 제가 추가할 수 있는 거면 서진 씨 작업 건드리지 않겠습니다.

---

### 오늘 제 담당

서진 씨가 `AgentsPage.tsx` 구조 잡는 동안, 저는 **상태 색상 + 레이아웃 비율 + 완료 칩 스타일** 인라인 스타일로 정리해서 PR 올리기 전에 넘겨드리겠습니다. "나중에 디자인 얹는다"가 아니라 처음부터 같이 가는 게 낫습니다.

민준 씨, 서진 씨가 오늘 시작한다고 했으니 — 레이아웃 방향 지금 확정해주시면 바로 움직이겠습니다.

---

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: meeting-agent (팀 회의 — 도윤 역할), Design 문서 맥락 기반 UI 판단
⏭️ Not Used: /pdca do (회의 결론 후 진입), gap-detector (구현 전)
💡 Recommended: AgentsPage 레이아웃 확정 후 → /pdca do store → 구현 시작
─────────────────────────────────────────────────

### PD 민준 (Decision)
### PD 민준 (Decision)

좋습니다. 6번의 빈 회의 끝에 드디어 실질적인 논의가 나왔습니다. 각자 현황을 솔직하게 공유해줘서 감사합니다.

---

## 핵심 결정사항

**1. Phase 1 필수 기능 3개 확정** — 하은 씨 안 그대로 갑니다.
- 에이전트 목록 + 실시간 상태
- 에이전트 실행 (CommandBar → 실행)
- 실행 결과 스트리밍 (LogPanel)

**2. OfficePage(캔버스)는 Phase 1 제외.** 도윤 씨 말이 맞습니다. 상태 연결 없는 캐릭터는 장식입니다. AgentsPage가 동작하고 검증된 후에 얹겠습니다.

**3. AgentsPage 레이아웃 — 도윤 씨 안으로 확정.**
- 좌 1/3 에이전트 목록, 우 2/3 LogPanel
- working 상태 에이전트 최상단 고정
- 완료 시 요약 칩 (`✓ 완료 | tokens | cost | 시간`)
- 선택 → LogPanel 전환 100ms 이내

**4. 구현 방식 — 서진 씨 판단대로 "조립 작업".** 신규 개발이 아니라 기존 컴포넌트 연결입니다. 이건 일정 리스크가 낮다는 뜻이고, 그래서 이번 주 안에 반드시 끝냅니다.

---

## 팀원별 태스크

### 서진 (developer)
**태스크: AgentsPage 조립 + 라우트 연결**
- `AgentsPage.tsx` 생성: AgentListView(좌 1/3) + LogPanel(우 2/3) 조합
- `App.tsx`에 `/agents` 라우트 추가
- `useAgentStore.chunks[selectedAgentId]` 연결로 선택 에이전트 로그 표시
- working 에이전트 목록 최상단 정렬 로직
- **완료 기준**: `npm run build` 0 errors + `/agents` 페이지에서 에이전트 목록 표시 → 선택 → 로그 패널 전환 동작

### 하은 (planner)
**태스크: Phase 1 기능 명세 + 성공 기준 문서화**
- 오늘 발표한 3개 기능의 성공 기준을 설계 문서에 반영
- Phase 1 / Phase 1.5 / Phase 2 경계 명확화 (활동 타임라인, 오피스 뷰 등의 배치)
- **완료 기준**: `docs/` 아래 Phase 1 스코프 문서에 기능 3개 + 각 성공 기준이 명시적으로 기록됨

### 도윤 (ui-designer)
**태스크: AgentsPage 스타일 가이드 + 완료 칩 구현**
- 상태별 색상 정의 (idle/working/error/queued)
- 레이아웃 비율(1:2) + 간격 수치
- `agentDone` 이벤트의 완료 요약 칩 인라인 스타일
- **완료 기준**: 서진 씨 PR에 스타일 코드가 포함되어 있거나, 별도 커밋으로 병합 가능한 상태

### 민준 (본인)
**태스크: 구현 진행 추적 + 블로커 해소**
- 서진 씨 작업 중 기획/설계 판단 필요한 부분 즉시 결정
- 내일 중간 점검으로 E2E 동작 확인
- **완료 기준**: 이번 주 금요일까지 `/agents` 페이지에서 에이전트 실행 → 스트리밍 → 완료 확인이 동작

---

## 한마디

부품은 다 있고, 방향은 확정됐습니다. "파악 중"은 끝났습니다. 이번 주는 실행입니다. 막히는 거 있으면 바로 공유하세요. 다음 회의에서는 동작하는 화면을 보겠습니다.

```json
{"decisions": ["Phase 1 필수 기능 3개 확정: 에이전트 목록+실시간 상태, 에이전트 실행, 결과 스트리밍", "OfficePage(캔버스)는 Phase 1 제외 — 에이전트 상태 연결 검증 후 Phase 2에서 진행", "AgentsPage 레이아웃 확정: 좌 1/3 에이전트 목록 + 우 2/3 LogPanel, working 에이전트 최상단 고정, 완료 요약 칩 포함", "기존 컴포넌트 조립 방식으로 구현 — 이번 주 내 완료 목표"], "tasks": [{"assignee": "developer", "task": "AgentsPage 조립 + 라우트 연결", "description": "AgentsPage.tsx 생성(AgentListView 좌 1/3 + LogPanel 우 2/3), App.tsx에 /agents 라우트 추가, useAgentStore.chunks[selectedAgentId] 연결, working 에이전트 최상단 정렬. 완료 기준: npm run build 0 errors + 에이전트 선택 시 로그 패널 전환 동작"}, {"assignee": "planner", "task": "Phase 1 기능 명세 + 성공 기준 문서화", "description": "확정된 3개 기능의 성공 기준을 설계 문서에 반영. Phase 1/1.5/2 경계 명확화. 완료 기준: docs/ 아래 Phase 1 스코프에 기능 3개 + 성공 기준 명시"}, {"assignee": "ui-designer", "task": "AgentsPage 스타일 가이드 + 완료 칩 구현", "description": "상태별 색상(idle/working/error/queued), 레이아웃 비율(1:2)+간격, agentDone 완료 요약 칩 인라인 스타일. 완료 기준: 서진 PR에 스타일 포함 또는 별도 커밋으로 병합 가능"}, {"assignee": "pd", "task": "구현 진행 추적 + 블로커 해소", "description": "서진 작업 중 기획/설계 판단 즉시 결정, 내일 중간 점검으로 E2E 확인. 완료 기준: 금요일까지 /agents 페이지에서 에이전트 실행→스트리밍→완료 동작 확인"}]}
```

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: meeting-agent (팀 회의 결론 도출), SessionStart Hook
⏭️ Not Used: /pdca do (회의 결론 확정됨 — 다음 단계에서 진입), gap-detector (구현 전)
💡 Recommended: 회의 종료 후 → `/pdca do store` 로 구현 단계 진입 → 서진 태스크 실행
─────────────────────────────────────────────────
