{
  "name": "radar-agent-office",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code multi-agent visualization dashboard",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "packageManager": "npm@10.9.4",
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  }
}
